{
  "name": "slow-cipher",
  "version": "0.1.0",
  "description": "Intentionally slow cipher",
  "main": "src/index.js",
  "author": "Marius Bieliauskas",
  "license": "MIT",
  "private": false,
  "type": "module",
  "scripts": {
    "dev": "esbuild demo=src/demo.js view=src/view.js --bundle --format=esm --target=esnext --servedir=public --legal-comments=none --outdir=public/js",
    "prod": "yarn dev --minify",
    "build": "esbuild src/index.js src/view.js --bundle --format=esm --target=esnext --minify --legal-comments=none --outdir=dist/",
    "get-random-salt": "./bin/run-slow-cipher.js randomHex"
  },
  "dependencies": {
    "crypto-js": "^4.1.1",
    "date-fns": "^2.28.0",
    "fast-sha256": "^1.3.0",
    "nanoid": "^3.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.14.10",
    "eslint": "^8.6.0",
    "eslint-config-airbnb": "^19.0.4",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.24.2",
    "prettier": "^2.4.0"
  }
}
